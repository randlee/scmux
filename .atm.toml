[atm]
default_team = "scmux"
identity = "team-lead"

[plugins.gh_monitor]
team = "scmux"
repo = "randlee/scmux"
notify_target = ["team-lead@scmux"]

# ── rmux: tmux session launcher ──────────────────────────────────────
[rmux]
session = "scmux"

[[rmux.windows]]
name = "agents"
layout = "even-horizontal"

[[rmux.windows.panes]]
name = "team-lead"
model = "sonnet"
env = { ATM_IDENTITY = "team-lead", ATM_TEAM = "scmux" }

[[rmux.windows.panes]]
name = "arch-ctm"
command = "codex -c features.codex_hooks=true --yolo"
env = { ATM_IDENTITY = "arch-ctm", ATM_TEAM = "scmux" }

[[rmux.windows]]
name = "monitoring"
layout = "even-vertical"

[[rmux.windows.panes]]
name = "logs"
command = "tail -f /tmp/atm.log"
