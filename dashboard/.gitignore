# Build artifacts (generated by compile.js / build.rs)
dashboard-compiled.js

# Node dev deps — not part of source
node_modules/
package.json
package-lock.json

# Large vendored JS — served from embedded binary in production
# Keep in git for preview.html development reference
# react.js
# react-dom.js
# babel.js (only needed for local dev transpile step)
babel.js
